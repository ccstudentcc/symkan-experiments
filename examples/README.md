# Examples

This directory contains small, versioned artifacts that help readers understand the expected outputs
without running the full experiments.

- `examples/benchmark_outputs/`: sample CSV outputs (e.g. multi-round benchmark summaries, symbolic parallel quick report).

These files are not used by the training/benchmark scripts at runtime.
